# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a newly initialized repository named "straimer". The .gitignore file indicates this will be a Node.js/JavaScript/TypeScript project.

## Setup Instructions

When code is added to this repository, this file should be updated with:
- Build commands (e.g., `npm run build`, `yarn build`)
- Test commands (e.g., `npm test`, `npm run test:watch`)
- Linting commands (e.g., `npm run lint`)
- Development server commands (e.g., `npm run dev`)
- Project architecture and structure
- Key design patterns and conventions used in the codebase
